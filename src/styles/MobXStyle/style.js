import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    checkBox: {
        flexDirection: 'row',
        alignItems: 'center',
        marginRight:20
    },
    checkBoxImage:{
        width:30,
        height:30,
        borderRadius:10,
    },
    textStyle:{
        fontSize:14,
    }
})

export default styles